Forward arrow, 16×16, 0.8px stroke — for in-page or next-item navigation (use `ArrowUpRight` for anything that leaves the site).

```jsx
<ArrowRight color="var(--text-link)" />
```

Props: `size` (default 16), `color`.

Header nav link. Three sit in a 32px-gap row to the right of the hairline rule.

```jsx
<nav style={{display:"flex",gap:32,alignItems:"center"}}>
  <NavItem active>Work</NavItem>
  <NavItem>About me</NavItem>
  <NavItem>Let's talk</NavItem>
</nav>
```

Plain black, no pill or background; hover is a fade to 60% opacity.

Intentional addition — no Figma symbol; promoted from the repeated nav text layer (node 9:168).

Header identity lockup: 52px rounded avatar (radius 8) + the name in serif 18. This stands in for a logo — the source file contains no brand mark.

```jsx
<Profile name="Breno Araujo." image="assets/breno-profile.png" href="/" />
```

Keep the trailing period in the name; that's how it is set in the file.

Intentional addition — no Figma symbol; promoted from the header avatar + name lockup in node 9:159.

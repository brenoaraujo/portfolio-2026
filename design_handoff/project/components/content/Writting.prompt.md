One row of the Writing list — year on the left, serif-bold title in the wide column, hairline underneath.

```jsx
<div style={{display:"flex",flexDirection:"column",gap:24}}>
  <Writting year="2020" title="Using the Lightning Decision Jam to surface problems and prioritize a quarter" href="#" />
  <Writting year="2017" title="Why User Experience Makes or Breaks Your Product" href="#" />
</div>
```

Same 1fr 2fr 1fr grid as `Experience`, so the two lists align down the page. Named `writting` after the source symbol — the double-t is intentional.
